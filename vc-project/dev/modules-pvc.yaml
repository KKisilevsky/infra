# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: modules-pv-volume
# spec:
#   storageClassName: "rook-nfs-share1"
#   accessModes:
#     - ReadWriteMany
#   resources:
#     requests:
#       storage: 200Mi
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: modules-pv-volume
  annotations:
    volume.beta.kubernetes.io/storage-class: "nfs-client"
spec:
  accessModes:
    - ReadWriteOnce
  #storageClassName: longhorn  
  resources:
    requests:
      storage: 200Mi